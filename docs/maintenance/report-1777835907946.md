# Technical Refactor Report: Refactor: Extract animation logic into separate functions

## Overview
- **Target File:** `script.js`
- **Impact Level:** High (Code Quality)
- **Status:** Documented / Pending Review

## Analysis
Current code mixes particle update and drawing logic within the `animBg` function. Extracting this into separate functions can improve readability, maintainability, and allow for easier modification of animation behavior.

## Proposed Optimization
**Line 143:**
```js
// Original Implementation
function animBg(){bgCtx.clearRect(0,0,bgC.width,bgC.height);if(currentScreen!=='s2'){particles.forEach(p=>{p.update();p.draw(bgCtx)});}

// Optimized Implementation
function updateParticles(){particles.forEach(p=>{p.update();});function drawParticles(ctx){particles.forEach(p=>{p.draw(ctx);});function animBg(){bgCtx.clearRect(0,0,bgC.width,bgC.height);if(currentScreen!=='s2'){updateParticles();drawParticles(bgCtx);}
```

## Rationale
By extracting the particle update and drawing logic, you can now modify the animation behavior independently without affecting the rest of the code.

---
*Verified by GitScan Professional Suite*